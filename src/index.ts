import type {Configuration, Plug, FetchResponse} from './plug';
import {GlobalPlug} from './plug';

export type {Configuration, Plug, FetchResponse};

/* eslint-disable-next-line import-x/no-default-export -- Should be default export */
export default GlobalPlug.GLOBAL;
